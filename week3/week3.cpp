//
// Created by 남기륭 on 2022/10/22.
//

int main() {
    // DFS 할 때 중요한 것
    // 그래프를 탐색할 때 f(0) -> f(1) -> f(2) => 실행 시 f(2) -> f(1) -> f(0)

    // 연구소 문제
    // 1. 무식하게 풀 수 있을까?
    // - 시간 복잡도 짐작
    // - 관계를 생각해서 그림으로 표현

    // 영화감독 숌
    // 1. 무식하게 풀기

    // 컴백홈
    // 1. 꺾어가는 것도 생각해야됨

    // 완전 탐색
    // 전의 테스트가 현재 실험에 영향을 안 미치도록

    // 백트래킹
    // 완탐 + 가지치기 - 안가도 될거 같은

    // 색종이 문제

    // 라인 스위핑

    // 투포인터
    // - 같이 시작
    // - 양 끝점

    // 그리디, 라인스위핑, 투포인터는 계속 시도가 중요한 부분
}